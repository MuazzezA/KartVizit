import { StyleSheet } from "react-native";
import { FONTS } from "../../../utils/constants";

export default StyleSheet.create({
  infoText: { ...FONTS.body2, textAlign: "center" },
  buttonContainer: {
    marginTop: 30,
  },
});
